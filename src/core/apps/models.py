"""Models for this app moved out during Fase 3 of REFACTOR-PLAN.md.

App/AppProcessScale/AppRunArtifact -> applications.models (Fase 3.4)
Service/ServiceType -> service_mgmt.models (Fase 3.5)
InteractiveRun* -> interactive_sessions.models (Fase 3.6)
CacheVersionIndex -> infrastructure.models (Fase 3.8)

This app (`core.apps`) still hosts views/serializers/admin/mixins/tasks code —
only the models themselves relocated.
"""
